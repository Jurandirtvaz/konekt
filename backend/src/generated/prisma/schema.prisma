generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") //Usaremos para queries rápidas
  directUrl = env("DIRECT_URL") //Usaremos APENA para migrations
}
